{
  "name": "git-pacman",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace packages/github-contributions && npm run build --workspace packages/grid && npm run build --workspace packages/svg-creator && npm run build --workspace packages/action",
    "test": "npm test --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
